# A sample Guardfile
# More info at https://github.com/guard/guard#readme


guard 'haml', input: 'Resources', output: 'Resources',
              haml_options: { format: :html5 }

guard 'sass', :input => 'Resources/stylesheets', :output => 'Resources/stylesheets'
